services:
  postgres:
    container_name: web-app-postgres
    build:
      context: .
      dockerfile: Dockerfile
    image: escala-postgres:16-alpine
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - web_app_pg_data:/var/lib/postgresql/data/
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      escala-network:
volumes:
  web_app_pg_data:

networks:
  escala-network:
    external: true
